{#
  Bloque: respira/page-hero — hero editable de página.
  Estructura como el hero de las páginas de detalle (.page-title): caja con
  border-radius y padding lateral. Imagen de fondo + overlay + contenido.
#}
<div {{ wrapper_attributes|raw }}>
  <div class="respira-page-hero__box" style="background-image: url({{ image }});">
    <div class="respira-page-hero__overlay"></div>
    <div class="auto-container">
      <div class="respira-page-hero__content">
        {% if subtitle %}<div class="h6 sub-title">{{ subtitle }}</div>{% endif %}
        {% if title %}<h1 class="title">{{ title }}</h1>{% endif %}
        {% if text %}<div class="text">{{ text|nl2br }}</div>{% endif %}
        {% if ctaLabel %}
          <a class="theme-btn btn-style-one" href="{{ ctaUrl ?: '#' }}">
            <span class="btn-title">{{ ctaLabel }}</span>
          </a>
        {% endif %}
      </div>
    </div>
  </div>
</div>
